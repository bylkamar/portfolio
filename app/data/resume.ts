/**
 * CV servi comme fichier statique depuis `public/`.
 *
 * Centralisé ici parce qu'il est référencé à plusieurs endroits (À propos,
 * navbar) : renommer le PDF ne doit demander qu'une seule modification.
 */
export const RESUME_PATH = "/assets/pdf/resume_ait_chikhoune_amer.pdf";

/** Nom du fichier tel qu'il arrive dans les téléchargements du visiteur. */
export const RESUME_FILENAME = "AIT-CHIKHOUNE-Amer-CV.pdf";
